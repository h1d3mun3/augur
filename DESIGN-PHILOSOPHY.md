# augur 設計思想の総合

設計ドキュメント群(ADR 0001–0011、INVARIANTS、セキュリティレビュー、README)を精読して総合した設計思想。全体を貫く思想は一言でいえば **「特権を持たず、失敗したら閉じ、境界だけを厳格に所有し、越えられない一線は隠さず文書化する」** サンドボックス思想である。11本のADRと10本のインバリアントは、ほぼこの一貫した価値観の派生として読める。

## 1. 最上位原理:sudo-free(ADR-0001)

すべての土台がこれ。核心は **「Privilege is not capability — it is blast radius(特権は能力ではなく爆発半径だ)」**。

- セキュリティツールが root を要求すると、**そのツール自身のバグが全部そのまま権限昇格バグになる**。閉じ込めるはずのエージェントに、逆に完全な制御を渡してしまう。
- 特権を持たないという固定した天井があるからこそ、INVARIANTS やレビューが「categorical(無条件)」な断言をできる。sudo があれば全部の保証に注釈が付く。
- そしてこの制約が **強制関数(forcing function)** として、より良いアーキテクチャを生んだ:`pf` ではなく host-only **vmnet**、特権ブリッジではなくユーザー空間の **gvproxy** netstack、カーネルファイアウォールではなくユーザー空間の **allowlist proxy**。

つまり「制約を守るために設計が洗練された」という因果を、彼ら自身が自覚的に記録している。

## 2. Fail-closed(閉じる方向に倒れる)

INVARIANTS の I1–I8 が体現する第二の柱。egress 制御は **「host-side, fail-closed, on-by-default」**。

- 設定が読めない → 起動拒否。空 → 全拒否。判定不能 → deny。boot self-test 失敗 → 環境ごと破棄。
- `AUGUR_EGRESS` に未知の値 → **on に倒す**(off ではなく)。
- ホットリロードが失敗 → 前のポリシーを維持(決して開かない)。

「迷ったら遮断」が全経路で徹底されていて、これはsudo-freeの「無条件保証」を成立させる技術的な裏付けになっている。

## 3. 境界(seam)の所有 — 「エージェント側はデータを宣言するだけ、サンドボックス核が全機構を所有・強制する」

ADR-0003(swappable-agent)の統治原理がこれ。将来 Claude Code を別エージェントに差し替えられるように **Anti-Corruption Layer(腐敗防止層)** だけを今入れ、多エージェント機構そのものは作らない。理由も一貫していて「機構を作れば C1–C7 の攻撃面が増えるだけで、消費者がまだいない」。

ADR-0002(Ollama)も同じ思想:**provenance(出自)はホスト側で強制する** — プロキシは平坦化された allowlist しか見えないので、信頼できないワークスペース層が IP を開けられないよう、ホスト側で IP ルールを剥ぎ取る。**「信頼できない入力に判断を渡さない、判断はいつも境界の内側」** という設計。

## 4. 受容した残余リスクは、隠さず文書化する(mitigateしない勇気)

最も特徴的な思想。**「塞げないものを中途半端に塞がず、天井を明示して止める」**。

- **ADR-0008(exfiltration ceiling)**:allowlist は *到達性* を制御するが *データ* は制御しない(TLSを終端しない)。だから許可済み・書き込み可能・認証付きホスト(例:`github.com` + 注入された `GH_TOKEN` → 攻撃者リポジトリへ push)経由の漏洩は防げない。これを塞ぐには augur が「トラフィック検査アプライアンス」という別クラスの重い装置になる必要があり、**「フルビルドしても detection止まりで prevention にならない=弱いリターン」**。だから **デフォルトでもopt-inでもやらない**。これは DLP/CASB という「augurの外に属する」仕事だ、と。
- **ADR-0001の残余**:Container の guest が host-only net 経由で `0.0.0.0` のホストサービスに届く問題。警告表示を試作・計測した上で、**「到達可能な集合の大半が非アクション可能なmacOSデーモンで、生むのはanxietyであってactionではない」** と判断して却下。
- **README の `.git/hooks`**:RWマウントされたワークスペースにフックを仕込めばホスト側であなたの権限で実行される — **「inherent, not a bug augur can close(augurが塞げる性質のバグではない)」** と最上位の残余リスクとして箱付きで明示。

判断の枠組みが繰り返し使われる:**「これは augur が所有すべき仕事の一部か?」**。サンドボックスとライフサイクルはそう、ディスク回収(ADR-0005 no-prune)や exfil検査(0008)や第二NIC はそうでない。

## 5. ラッパーは価値を足すときだけ存在する(YAGNI / 薄い皮の否定)

- **ADR-0005(no prune)**:`augur prune` を作って撤回。「呼び出しを1:1で転送するだけでスコープ/安全/UXを何も足さないラッパーはコストに見合わない」。ホットパスの副産物(rebuild時の孤立レイヤ)だけ自動掃除し、稀な手動掃除は素の `container prune` を **文書化** して済ませる。
- **ADR-0004(no --worktree)**:専用サポートを足すと、どの修正も blast radius を広げるか、共有 `.git` のRWマウント(=ホスト脱出経路)を開くか、コピーして worktree の存在意義を消す(なら `git clone` と等価でコード追加ゼロ)。だから足さない。

## 6. 対称性 — 2つのバックエンドを同じメンタルモデルに寄せる

Container mode と macOS VM mode の挙動差を、正当な理由がある限り消していく動き。

- **ADR-0010**:Container も `down` で停止・保持し `up` で **reconcile(再照合)** する。macOS と対称に。ただし重要な洞察が **「persistence自体は危険ではない(macOSが証明済み)。危険なのはmacOSのpersistenceを、その reconcile-on-up 機構なしにコピーすること」**。だから fingerprint でconfigドリフトを検知して drift時はクリーン再作成、boot self-test が reuse経路も含め毎 `up` をゲート。
- 逆に **ADR-0006** は「対称化しない正当な非対称」も記録:macOS VM は事実上 Xcode専用で、DerivedData/Simulator/SPM の再構築が高価だから clone を保持する。Container は bind-mount 外に高価な状態を持たないので破棄が正しい。**「対称にすべき所は対称に、非対称が正しい所はその理由ごと記録」**。

## 7. サンドボックスこそが信頼境界 — だから内側の摩擦は削る

- **ADR-0011(workspace trust seed)**:「augur をこのリポジトリで実行するという operator の決定が、すでに信頼の決定そのもの」だから、guest 内の信頼ダイアログは冗長。ゲスト側の合成 `~/.claude.json` に trust を seed する。ただし **ホストの本物の `~/.claude.json` は決して読まない/マウントしない**(host-env-exposure-review が裏付け:認証は env で値注入、履歴はホストの `~/.claude` 外に置き、敵対的ゲストが operator の閲覧する transcript を偽造できないように)。

境界の外(ホスト)への漏れは徹底的に絞り、境界の内側の無駄な摩擦(信頼ダイアログ、パスワード入力 ADR-0007)は削る、という非対称な力の入れ方。

## 8. ドキュメント自体の設計思想

メタレベルでも思想が一貫している(各READMEが明文化):

- **ADR = 恒久的な *根拠*(なぜ、を再litigateさせない)** — 決定が本当に変わったら上書き。
- **security-reviews = 不変の *時点スナップショット*(台帳)** — 一度書いたら編集せず、新ファイルで supersede。最新1本を読めば現状がわかり、有効な残余は最新が carry-forward する責任を負う。
- **INVARIANTS.md = テストで守る *契約*** — 通常の修正はプロセ文を書き換えず「契約をgreenに保つ」。契約を変えるときだけ新スナップショットが要る。

**「根拠 / 時点の記録 / テストされる契約」を混ぜない** ことで、それぞれが別の所有者・別の更新頻度で腐らない、という設計。最新の 2026-07-23 レビューが「invariantのテキストは一字も変えていない(実装だけ強化)」と自己申告しているのが、この分離がワークしている証拠。

---

## まとめ:一文にすると

> **「特権を取らない(sudo-free)という固定した天井の上に、fail-closed でホスト側が全機構を所有する egress サンドボックスを組み、越えられない一線(exfil、.git/hooks)は塞ぐふりをせず明示して止める。そして『なぜそう決めたか』を、腐らない形で分離して記録し続ける」**

augur は機能の寄せ集めではなく、**"prompt-injectされ得るコーディングエージェントを、信頼された単一ユーザーのホストから *contain(封じ込める)* する。ただし exfil-proof にはしない"** という単一の脅威モデルから全設計が演繹されている。「contain, not exfiltration-proof」がその合言葉。

---

*補足:`docs/ollama-llm-endpoint-design.md` と `docs/claude-worktree-support-design.md` は現在リダイレクトスタブ相当で、実体はそれぞれ ADR-0002 / ADR-0004 に移設済み(不変スナップショットからの過去リンクを生かすためスタブを残す運用ルール通り)。*
